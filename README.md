# TRNSYS Releases

Bundled TRNSYS versions for end-user installation.

Each release is a complete TRNSYS distribution packaged as a single downloadable archive, containing runtime binaries, standard types, and vendor components.
`trn` downloads from this repo during installation.

Releases are assembled from tagged versions of individual component repos.
